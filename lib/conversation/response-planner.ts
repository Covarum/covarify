export const transactionAnswer = (input: { intent: "transaction_count" | "transaction_total" | "transaction_list"; merchant: string; count: number; total: string; coverage: string }) => input.count === 0 ? `I didn't find any ${input.merchant} payments in ${input.coverage}.` : input.intent === "transaction_total" ? `You paid ${input.merchant} ${input.total} across ${input.count} ${input.count === 1 ? "payment" : "payments"} in ${input.coverage}.` : input.intent === "transaction_list" ? `I found ${input.count} ${input.merchant} ${input.count === 1 ? "payment" : "payments"} totaling ${input.total} in ${input.coverage}.` : `I found ${input.count} ${input.count === 1 ? "payment" : "payments"} to ${input.merchant} totaling ${input.total} in ${input.coverage}.`;
