import { signUp } from "../auth/actions";
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="mx-auto max-w-md p-8"><h1 className="text-3xl font-semibold">Create account</h1><p>Covarify is currently a controlled beta.</p>{p.error&&<p role="alert">{p.error}</p>}<form action={signUp} className="grid gap-4 mt-6"><input required name="email" type="email" aria-label="Email"/><input required minLength={12} name="password" type="password" aria-label="Password"/><button>Create account</button></form></main>}
